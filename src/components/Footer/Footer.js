import React from 'react'
import styles from './Footer.module.css'
export const Footer = () => {
  return (
    <div className={`bg-primary text-center text-white ${styles.footer}`}>&copy; Rights belongs to me.</div>
  )
}
